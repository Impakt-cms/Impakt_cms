(function () {
    'use strict';

    angular
        .module('app')
        .controller('contact_controller', function contact_controller($scope, $log) {
			console.log("Contact controller loaded.");
        });
})();